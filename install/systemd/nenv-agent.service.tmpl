[Unit]
Description=netenv node check-in agent
After=network.target

[Service]
Type=simple
ExecStart=%h/bin/nenv-agent --server __NENV_SERVER__ --token __NENV_TOKEN__ --hostname __HOSTNAME__ --interval 60s
Restart=on-failure
RestartSec=10

[Install]
WantedBy=default.target
