# friday-drive-mounts

SSHFS mounts of Friday (10.2.0.102) internal drives to the laptop + Mini, with share + mounts doctors.
Full docs: see the `friday-drive-mounts` Hermes skill. Source repo: github.com/b3nnb/friday-drive-mounts (recipe + unit files also versioned there).

Run: `gogitops recipe run friday-drive-mounts` — per-node steps are label-gated (primary-compute / laptop / lora-training).
