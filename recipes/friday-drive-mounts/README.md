# friday-drive-mounts

SSHFS mounts of Friday (10.2.0.102) internal drives to the laptop + Mini, with share + mounts doctors.
Docs: the `friday-drive-mounts` Hermes skill (manual gates + pitfalls).
This recipe IS the source of truth for scripts + unit files — the earlier standalone github.com/b3nnb/friday-drive-mounts repo was deleted (duplicate).

Run: `gogitops recipe run friday-drive-mounts --hostname <node>` — per-node steps are label-gated (primary-compute / laptop / lora-training).
